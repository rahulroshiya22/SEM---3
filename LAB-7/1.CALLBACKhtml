<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>CallBack function</title>
</head>
<body>
<script type="text/javascript">

	a = parseInt(prompt("Enter first number"));
	b = parseInt(prompt("Enter second number")) ;
	function calc(a,b,add,sub,mul,div)
    {
	 	add(a,b);
	 	sub(a,b);
	 	mul(a,b);
	 	div(a,b);
	 }
	 calc(a,b,add,sub,mul,div);
	 function add(a,b)
     {
	 	console.log("Add = " + (a+b) );
	 }
	function sub(a,b0)
    {
	 	console.log("Sub = "+(a-b));
	 }
	 function mul(a,b)
     {
	 	console.log("Mul = "+(a*b));
	 }
	 function div(a,b){
	 	console.log("Div = "+(a/b));
	 }
</script>
</body>
</html>
